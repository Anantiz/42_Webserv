#pragma once
// This file contains the general includes that are used in the project
// as well as pre-declarations of custom classes and functions
#include "webserv.hpp"

/*
	** Simple class to manage all the clients conections
	
*/
class clientManager
{
private:
	/* data */
public:
	clientManager(/* args */);
	~clientManager();
};