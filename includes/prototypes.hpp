#pragma once

// cluster
class Cluster;

// cluster/server
class Server;

// cluster/server/location
class Location;

// client_event
enum e_method;
enum e_protocol;
enum e_conection_status;
struct s_client_event;

// utils
class logs;