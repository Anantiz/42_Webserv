#pragma once

// cluster
class Cluster;

// cluster/server
class Server;

// cluster/server/location
class Location;

// client_event
class ClientEvent;
struct s_client_event; // Not part of the class for more flexibility

// utils
class logs;